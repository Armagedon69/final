version: 2

models:
  - name: stg_order
    columns:
      - name: ORDER_ID
        tests:
          - not_null
          - unique

  - name: stg_item
    columns:
      - name: ORDER_ID
        tests:
          - not_null
          - unique

          
          